node_modules/
dist/
coverage/
tsconfig.tsbuildinfo
.claude/settings.local.json
.DS_Store

# Which spreadsheet the Node host points at — a local choice, not a shared one.
nodeHost.config.json

# OAuth client secrets live outside the repo; this is belt-and-braces.
client_secret*.json
.clasprc.json*
