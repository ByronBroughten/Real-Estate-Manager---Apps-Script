node_modules/
dist/
coverage/

# Agent tooling, managed by skill installs rather than by this project.
.agents/
.claude/

# Written by `npm run gen:configs` in its own JSON-ish shape (quoted keys, no
# trailing commas). Formatting them makes every regeneration a spurious diff.
src/01_generatedConfigs/sheetConfigs.ts
src/01_generatedConfigs/columnConfigs.ts
src/01_generatedConfigs/valueConfigs.ts

# Hand-curated prose; prettier rewrites *emphasis* and re-pads wide tables.
*.md
