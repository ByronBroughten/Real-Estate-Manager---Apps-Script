# Claude Code guardrails

Mechanics of the Claude Code hooks and project agent. Why they are shaped this way: [`docs/agent-behavior-design.md`](./agent-behavior-design.md).

Eight Node hooks in `.claude/hooks/`, registered in `.claude/settings.json`: three gate a call (the Bash-read guard, the pinned-target guard and the STYLE gate) and five add feedback or a reminder. All fail open except where noted. Session state lives in `$TMPDIR/claude-guardrails/`.

**Start every session at the repo root.** The settings, hooks and agent load only from the root `.claude/`, so a session started inside `packages/*` runs with none of them and every call asks. Each package's `CLAUDE.md` says so; restart at the root.

## The hooks and the project agent

`.claude/settings.json` registers eight Node hooks in `.claude/hooks/`, TypeScript that `node` runs by stripping types (erasable syntax only, `.ts` imports, nothing from `packages/`), fail-open (bad input allows the call) unless noted, plus one project agent. `bashReadGuard.ts`, `pinnedTargetGuard.ts` and `styleGate.ts` gate a call; the others add feedback or a reminder.

- **`bashReadGuard.ts`** (PreToolUse, Bash) denies a Bash read of `columnConfigs.ts`, unless it is a grep or a `sed -n` range of at most 150 lines. It also denies a whole-file dump (`cat`, unbounded `head`/`tail`, `sed` without `-n`, `sed -n '1,$p'`) of a repo file over 150 lines. The deny message names the alternative. Piped input, small files, anything under a `.probe/`, `dist/`, `coverage/` or `node_modules/` folder at any depth, and anything outside the repo are not guarded. The `columnConfigs.ts` it guards is every package's, found through the `generatedDir` in each `sheets.config.json` (`lib/sheetsConfigs.ts`). `lib/bashReads.ts` is tested beside it. The classifier (`lib/bashReads.ts`) is shared with `readCountNudge.ts`.
- **`pinnedTargetGuard.ts`** (PreToolUse, Bash and gsheets `update_cells`/`batch_update_cells`/`create_sheet`) keeps the dev standing yes pinned to the dev spreadsheet. It turns an allowed `dev:*` write (`dev:gen:configs`, `dev:chore … --send`, `dev:build`, `dev:push`, `dev:run`) into ask while a pinning file (both `sheets.config.json` files, the guard and its libs) has uncommitted changes. It allows a gsheets write only when `spreadsheet_id` is the dev ID from `packages/framework/sheets.config.json` and the pinning files are clean, and otherwise asks. An unreadable git state or config asks. A crash fails open on the Bash side, leaving the allow rule, and closed on the gsheets side, leaving the default ask. The decision is `lib/pinnedTargets.ts`, tested beside it. The isolation table it enforces: [`docs/how-it-runs.md`](./targets-and-gates.md#targets-dev-and-app).
- **`generatedEditWarning.ts`** (PreToolUse, Edit/Write) warns, without blocking, before an edit inside any package's `generatedDir`, naming that package's `*:gen:configs`: regenerate instead, floor entries included: fix the live tab or the seed ([`docs/generated-data.md`](../packages/framework/docs/generated-data.md)).
- **`agentsCostNudge.ts`** (PreToolUse, Edit/Write) reminds, without blocking, on the first edit to the root `AGENTS.md` in a session, once: every byte of that file loads on every turn, so add only what changes behavior on nearly every task ([`prose-files.md`](./agents/prose-files.md#what-an-addition-to-agentsmd-costs)). Nested `AGENTS.md` files draw nothing. The decision is `lib/agentsCostNudge.ts`, tested beside it.
- **`styleGate.ts`** (PostToolUse, Read; PreToolUse, Edit/Write) records a Read of `docs/style.md` that covers the whole file (no `offset` or `limit`, or bounds reaching its last line), and denies an edit to a code file ESLint lints (any `.ts`, `.js` or `.mjs` outside `dist/`, `coverage/`, `node_modules/` and `.claude/worktrees/`) until that read is recorded for the session. **The gate set is the lint set minus each package's `generatedDir`**, whose files are left to `generatedEditWarning.ts`; it is `isInLintSet` in `lib/lintSet.ts`, shared with `lintFeedback.ts`. The decision is `lib/styleGate.ts`; both are tested beside them.
- **`lintFeedback.ts`** (PostToolUse, Edit/Write) runs after the Prettier step, in the same settings entry so the order holds: `eslint --fix` on the edited file, with `@typescript-eslint/no-unused-vars` off for that run so an import added one edit before its use isn't flagged, then Prettier again if the fix changed the file. It adds each error left, as `line:col rule message`, as context, and says nothing for a clean file. It lints only files in the lint set (`lib/lintSet.ts`), and fails open on a crash, a timeout (20 s) or output it can't parse. `npm run lint` stays the final gate. The feedback text is `lib/lintFeedback.ts`, tested beside it.
- **`readCountNudge.ts`** (PostToolUse on Read/Grep/Glob/Bash; reset on UserPromptSubmit) counts reads per turn: Read, Grep, Glob, and Bash calls the classifier calls reads. Edits, `tsc` and test runs are not counted. At 15 reads, and every 10 after, it reminds Claude to write findings down with `file:line`. Each subagent has its own count, and `repo-explorer` is exempt.
- **`contextSizeNudge.ts`** (UserPromptSubmit) estimates context from the transcript's last main-thread usage figures, falling back to bytes ÷ 4. It warns once past ~400k and once past ~1M; the second warning asks for a handoff ([`docs/agents/planning.md`](./agents/planning.md#handoffs)) and a fresh session.
- **`.claude/agents/repo-explorer.md`** is a read-only (Read/Grep/Glob) Sonnet agent for sweeps of about 5+ files. It returns `file:line` plus verbatim quotes.

## Per-session state

Per-session state (the read log, which size warnings have fired, whether docs/style.md was read, and whether the AGENTS.md cost reminder fired) lives in `$TMPDIR/claude-guardrails/`, keyed by session id.
