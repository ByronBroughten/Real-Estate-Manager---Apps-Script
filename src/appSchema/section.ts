type SectionBase = {
  headerIndex: number; // maybe always 1
  sheetId: string; // I'll primarily, or perhaps exclusively, use sheet ids, data ranges, and table notation, aside from dropdown options.
  idPrepend: string; // e.g. p- for property, u- for unit, h- for household.
};
